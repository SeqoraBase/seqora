# Private planning and research — never committed
docs/

# Local dev config
CLAUDE.md
.claude/

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/lcov.info
# NOTE: contracts/.gas-snapshot is committed so CI's `forge snapshot --check` has a baseline.

# Node / frontend
node_modules/
.next/
dist/
build/

# Generated at prebuild from SPEC.md at repo root — do not hand-edit
frontend/src/content/spec.md

# Env / secrets
.env
.env.*
!.env.example
!.env.local.example

# Foundry snapshot backups
contracts/.gas-snapshot.old

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Anvil / local chain
.anvil/
anvil.log
